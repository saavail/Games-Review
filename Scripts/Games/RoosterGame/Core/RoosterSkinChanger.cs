using Core;
using Enums;

namespace Games.RoosterGame.Core
{
    public class RoosterSkinChanger : SkinChanger
    {
        protected override GameType GameType => GameType.RoosterRunner;
        protected override void OnStart(GameData gameData)
        {
            
        }
    }
}